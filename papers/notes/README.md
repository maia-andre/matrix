# notes/ — notas curtas, uma descoberta cada

Um paper é uma **síntese**. Uma nota é o **registro** de um achado, escrito
enquanto o código que o produziu ainda existe.

## Por que notas, e por que agora

O argumento não é de organização, é de **preservação de evidência**. A nota 01
nasceu no mesmo dia em que o mostrador `modelo` foi consertado — e o conserto
**destruiu a evidência**: o `main.c` mudou, o `datasets/seed7.csv` foi
regenerado, e o famoso `modelo = 1,000` para um bloco sem modelo de mundo deixou
de ser reproduzível a partir do estado atual do repositório. Sem a nota (e sem o
script que a acompanha), o achado sobreviveria só como anedota.

Daí as três regras:

1. **Uma descoberta por nota**, como é um degrau por commit.
2. **Toda nota é reproduzível.** Cada uma vem com um script que regenera seus
   números a partir do `main.c` — inclusive de uma *versão passada* dele, via
   `git show <commit>:main.c`. Nada de `experiment_001.c`: há **um `main.c`
   canônico** e as ablações são patches aplicados a uma cópia temporária (ver
   [`ROADMAP.md`](../../ROADMAP.md), seção "Engenharia").
3. **Toda nota diz a qual paper ela serve.** Notas que não sobem para lugar
   nenhum viram diário.

## O que uma nota pode ter e um paper não

**Hipóteses mortas.** Papers lavam a cronologia: apresentam a conclusão como se
ela tivesse sido óbvia. A nota registra que três explicações sucessivas para o
horizonte de planejamento foram falsificadas antes da quarta se sustentar. Num
projeto cuja tese é sobre o que uma medida *carrega*, hipótese morta é dado.

## Formato

Cabeçalho com **data**, **commit do `main.c`** a que os números se referem, e
**como reproduzir**. Depois: resumo, aparato, resultados, ameaças à validade, o
que ficou em aberto. Curto. Se passar de duas páginas, provavelmente são duas
notas.

## Índice

| nota | achado | serve ao paper |
|---|---|---|
| [01](./01-quatro-modos-de-errar.md) | os quatro mostradores da bateria falham de quatro maneiras independentes; `modelo` consertado | 1 — metrologia da mente |
| [02](./02-o-teto-de-nascimentos.md) | um teto de nascimentos congelava a evolução; havia uma regra de senioridade implícita; `hor_m` não é identificável sozinho | 2 — bem posicional (+ erratas do 1) |
| [03](./03-a-evolucao-extingue-a-agencia.md) | `agencia` consertada (varredura do domínio, não dois pontos); a queda dela não era defeito de régua — o **reflexo fixa contra o agente** | 1 — metrologia (+ `FILOSOFIA_v3`) |
| [04](./04-o-automodelo-era-um-modelo-do-outro.md) | `automodelo` era observação, não intervenção; escalava com `ANTECIPACAO`; e media o **outro** (zero para um eremita). Vira `modelo_do_outro`, intervenção ancorada | 1 — metrologia da mente |
| [05](./05-phi-media-o-segundo-motivo.md) | `phi` era **infalseável** (nenhuma ablação a zerava) e a suspeita "= profundidade" era artefato de janela; media o **segundo motivo**. Redefinida: menor distância aos módulos isolados; **a evolução extingue a integração** | 1 — metrologia (+ `FILOSOFIA_v3` §2) |
| [06](./06-o-interprete-leigo.md) | `relato` entregue com **pré-registro antes do código**; κ do intérprete leigo ≈ 0,67; **confabulação selvagem** em ~21% das ações negadas; P5 falhou — o eremita fica **mudo** (self de um motivo só não tem o que relatar) | 1 — metrologia (+ `FILOSOFIA_v3` §2 e §5) |
| [07](./07-o-dedo-do-espectador.md) | Bandersnatch forçado: a tabela de Gazzaniga vira **uma linha por arquitetura** (lê-ação confabula, lê-plano não percebe, monitor detecta **sem autoria**); dose-resposta do κ; o dedo sem rastro é **indetectável por princípio** | 1 — metrologia (+ `FILOSOFIA_v3` §4 e §5) |
| [08](./08-o-sinal-e-a-mentira.md) | o `relato` vira **causal**: sinal no lugar da telepatia do nv5, estratégia herdável; a **honestidade evolui** (ESS sem multa), sobra ~10% de blefe (polimorfismo); silêncio = eremita epistêmico. S2/S6 erraram a direção — achados | 1 — metrologia (+ Paper 3 futuro; `FILOSOFIA_v3` §3) |

| [09](./09-o-self-ja-estava-la.md) | o auto-modelo **já estava no código** (`food -= garfada`: "a célula empobrece porque EU vou comer"). `autocausa` é o **único mostrador que o eremita possui** — mas ele tem só **1/5** do de um bloco acompanhado: a bifurcação da escada vira uma **razão**, não um vencedor. `horizonte=1` o zera exato (**o self exige um futuro**); e a mesma seleção que **apaga a agência constrói o self**. Errata: a `agencia` do eremita não era "0 exato" — tinha um **piso de arredondamento** em float32 | 1 — metrologia (+ `FILOSOFIA_v3` §2, §3 e Apêndice A) |
| [10](./10-o-zero-estrutural.md) | a dívida da nota 09 §5 quitada: `modelo`, `phi` e `relato` recomputados em `double` — **os três zeros são zeros nas duas precisões**, a nove casas, no máximo e fora do clamp. E o piso ganha mecanismo: **float32 não inverte ordens, cria empates** — só vaza a sonda que conta trocas de argmax sob desempate (`agencia`); zeros por identidade, monotonia ou quociente igual são **estruturais**. Regra de desenho para mostradores futuros | 1 — metrologia (+ Apêndice A da `FILOSOFIA_v3`) |
| [11](./11-a-replicacao.md) | **50 seeds para tudo que foi afirmado com 3**: os dez zeros do Apêndice A com **0 violações em 500 corridas**; `autocausa` sobe × `agencia` cai em **50/50**; razão do eremita 4,78 ± 0,67; eremita mudo 0,0046 ± 0,0005. O piso fora do eremita fecha: 1,4·10⁻⁷, sem direção — é fenômeno do regime eremita. Moveu: extinção pv0 51–121 (errata na 01); `modelo_do_outro` recalibra a 0,27 ± 0,05 (efeito da nota 08); e um erro do próprio pré-registro fica registrado (comparei `hon_f` de 3000 ticks com número de 30 000). Agregados em `datasets/replicacao50.csv` | 1 — metrologia (a tabela média ± dispersão) |
| [12](./12-a-janela-longa.md) | **a janela longa, 30 000 × 50**: `hon_f` = 0,898 ± 0,028 (a nota 08 estava certa no horizonte certo); o traço congelado sem uma exceção em 150 corridas (`fesp` segura phi e agência; ctl/`fdep` desabam); o artefato de janela ganha **controle por intervenção** (congelar o traço faz a corr *cair* com a janela). E a errata de mecanismo da nota 09 P4: com horizonte **pregado**, a `autocausa` sobe *mais* — o motor do self é o **estreitamento do motivo**, o mesmo `peso_espaco → 0` que apaga a agência. **Um processo, não dois.** Agregados em `datasets/janela30k.csv` | 1 — metrologia (+ `FILOSOFIA_v3` §2/§3) |
| [13](./13-o-fatorial.md) | **o fatorial de L5 fechado**: `fambos` (motivo *e* horizonte pregados) é a quarta célula. Com os dois motores desligados a `autocausa` quase não sobe — Δac +0,022 ± 0,009, 0/50 seeds acima do bar do "terceiro motor" (F1 ✅, F2 não disparou): o crescimento do self **é** a evolução desses traços. E a não-aditividade da nota 12 ganha número: **interação = −0,053**, do tamanho dos efeitos principais — o horizonte **inverte de sinal** conforme o motivo, é amplificador do motor, nunca motor autônomo. Agregados em `datasets/fatorial30k.csv` | 1 — metrologia (+ `FILOSOFIA_v3` §2) |
| [14](./14-o-torneio.md) | **o torneio 12×12 do horizonte**: 66 pares, só o horizonte varia. Dominância **transitiva** (deeper vence os 66 duelos; `h=12` vence os 11) — o **ESS é o teto**, sem freio endógeno antes da parede. Mas a margem **satura** (1,000 → 0,56) no joelho do desconto (profundidade efetiva `1/(1−δ)`), sem zerar: gradiente decrescente e sempre positivo, por isso a corrida vai até `HORIZONTE_MAX`. Achado não previsto: transição **fixação → polimorfismo** em `h≈4` (exclusão abaixo do teto do desconto, coexistência acima). Agregados em `datasets/torneio.csv` | 2 — bem posicional (ESS) |
| [15](./15-o-imposto.md) | **a dose-resposta `h*(c)`**: o imposto pigouviano sobre a cognição (custo por profundidade efetiva). A profundidade evoluída **desce ao ótimo de grupo** (`h=1`) com o custo — a Rainha Vermelha desarmada (D1/D2 ✅). O imposto morde o que é **identificável**: `hor_m` é ruído (sd até 15× a da profundidade), a profundidade efetiva responde limpa — por isso taxar a profundidade, não o horizonte declarado (D3 ✅). Achado não orçado: a receita é **queimada**, não redistribuída → alinhar a escolha custa ~35% da população; alinhamento ≠ bem-estar. Agregados em `datasets/imposto.csv` | 2 — bem posicional (`h*(c)`) |
| [16](./16-a-varredura-do-desconto.md) | **a varredura de δ derrubou a nota 14**: o "ESS = teto" era artefato do δ=0,80 — em δ=0,95 a escada **inverte** (h=9 vence h=12, `t=−11`): **ESS interior**, e o ótimo **desce** com δ (≥12 → ~10 → ~7–8). A transição fix→polim **não** anda (h=4 em todo δ): a nota 14 §3 fundia duas escalas. `1/(1−δ)` é aproximação de δ baixo — diz quando a profundidade extra é *invisível*, não quando é *nociva*. §4 propõe (como **hipótese**) o desconto como regularizador. Agregados em `datasets/desconto.csv` | 2 — bem posicional (ESS) |
| [17](./17-ruido-ou-teimosia.md) | **ruído, não teimosia**: populações de **tipo único** decidem — o déficit do h=12 em δ=0,95 é **absoluto** (−5,5 blocos *sem rival de outro tipo*, `t=−4,3` pareado; +31,4 de comida em pé), com **dose-resposta em δ** (+20 → +45 → +74 contra h=1). O desconto é um **regularizador**; **o pico de colheita é h=2** — planejar fundo nunca colheu melhor neste mundo. Erros meus no registro: teste não-pareado num desenho pareado; `energia_media` confundida por densidade. Agregados em `datasets/tipo-unico.csv` | 2 — bem posicional (mecanismo) |
| [18](./18-o-erro-do-plano.md) | **o elo que falta, fechado**: a sonda do erro de previsão a k passos. `corr(ĝ_k, r_k)` = 0,60–0,73 em k=0 e ≤ 0,19 de k=1 em diante — **k\* = 0 em todas as condições povoadas, 8/8 seeds**. A premissa morre no primeiro replanejamento (fprem(1) ≈ 0,05): o plano é **um passo com uma cauda que nunca é vivida**. Âncora: no eremita, premissa cumprida ⇒ erro **0 exato** em double (o modelo É a regra do mundo). Achado E4: o erro **piora** com δ (cauda anticorrelacionada em 0,95) — o comportamento fundo fabrica o próprio ruído. Fronteira honesta: mede o plano como *previsão*, não como *ranking* (sonda ordinal declarada, não construída). Agregados em `datasets/sonda-erro.csv` | 2 — bem posicional (fecha o §8) + 1 — metrologia |
| [20](./20-o-invasor-raro.md) | **o `h*` não é um ESS — é um ponto de ramificação**: o invasor-raro (`p0=0,1`, linhagem pura) refuta a resistência do ESS estrito (I1: j=4,6,7,12 crescem contra o residente `h*`) mas confirma a atração (I3: `h*` raro invade os **dois** lados). Convergence-stable **+** invasível = ramificação: o `h*` é o centro de um **polimorfismo protegido** (o `12` raro cresce contra o `h*` **e** vice-versa → coexistem), não o topo de uma colina. Explica o "`hor_m` é ruído" da nota 15 (o ponto singular seleciona a favor da variância). I4: raro × 50/50 divergem onde o par coexiste (freq-dependência). O caráter do ponto muda com δ: teto quase-ESS em 0,80 → ramificação interior em 0,95–0,96. Agregados em `datasets/invasor-raro.csv` | 2 — bem posicional (ESS → ramificação) |
| [19](./19-a-grade-fina.md) | **a grade fina do desconto**: o ESS interior localizado em curva. `h*(δ)` desce **monotonicamente** — teto/platô (0,88) → 10 → 9 → 8 → 8 → 7 (0,96) — enquanto `1/(1−δ)` **sobe** de 8,3 a 25: as duas andam em **direções opostas** (G3). O ótimo deixa o teto entre **δ=0,88 e 0,90** (G2 alternativa (b)); o topo não pula, **tomba** de neutro (platô em 0,88, o "inofensivo e inútil" da nota 16) a ladeira punida (`t` até −20 em 0,96). Replica: a transição fix→polim **não** anda (h≈3–4 em todo δ, G4); e a dominância transitiva "o mais fundo vence" da nota 14 **inverte** acima do joelho (h=9 bate h=12 diretamente de δ≥0,90). G0: 160/160 idênticas a `desconto.csv`. Nota de método: a seed 7 sozinha dava uma história limpa e errada — o platô só existe na média das 8. Agregados em `datasets/grade-fina.csv` | 2 — bem posicional (ESS interior) |
| [21](./21-o-imposto-que-recicla.md) | **o imposto que recicla**: dividendo per-capita em vez de queima. R1 — o alinhamento (profundidade → ótimo de grupo) **sobrevive** ao rebate, quase intocado (diferença ≤ 0,16 contra o queimado, em todo `c`). R2/R3 — o bem-estar **também** sobrevive: no `c=0,15` que alinha, a população queimada é 66% do baseline; a reciclada é **102%**. R4 — o pré-registro previa um possível resíduo de custo (redistribuição para sobreviventes perto do teto de reprodução); mediu-se o **oposto**: um excedente de ~2% em todo `c ≥ 0,01`, que converge (candidato, não isolado) com a externalidade posicional de ~2,2% já medida na paisagem de grupo do §3 do Paper 2. Alinhar a escolha, sem queimar a receita, sai **de graça — e um pouco mais**. Agregados em `datasets/reciclagem.csv` | 2 — bem posicional (corrige §7) |
| [22](./22-a-sonda-ordinal.md) | **a sonda ordinal fecha a fronteira da nota 18**: o plano falha como previsão (`k*=0`) mas ORDENA as até 9 células candidatas muito melhor que o acaso — discordância de pares 0,08–0,20 (acaso=0,5) em toda condição povoada, argmax bate 3,8–5,8× o acaso, reconstrução da decisão real 100,000% exata (74M decisões, 8/8 seeds). A discordância quase não piora de `h=4` a `h=12` (\|Δ\|≤0,015, contra o colapso de `corr(g_k,r_k)` já em k=1) — candidato a mecanismo para o pico de colheita `h=2` da nota 17. A decisão completa rastreia o realizado melhor quando faminto (`t` até 29, 8/8 seeds); no eremita (controle algébrico: `espaço` vira constante, `rank(u)≡rank(m)` por afinidade), a mesma comparação **inverte** (`t=−29,5`), como a álgebra exige. Agregados em `datasets/sonda-ordinal.csv` | 2 — bem posicional (fecha §8 do Paper 2) |
| [23](./23-o-teste-de-bimodalidade.md) | **o ponto de ramificação vira polimorfismo real — mas só no fundo em que foi medido**: no mesmo fundo pregado da nota 20 (`uni_h_livre`), o horizonte livre para mutar em 1..12 sustenta massa substancial nos dois lados do `h*` em **8/8 seeds** a partir de δ=0,90 (saturação total), contra 4/8 em δ=0,80 (previsto ≤2/8 — a dominância do teto das notas 14/16 não é o mesmo teste que a invasão-rara da nota 20, e o desenho não separa ramificação fraca de carga de mutação). Mas na evolução **de verdade** (todos os traços livres), **nenhuma seed é bimodal** (0/8) — cada corrida converge para um pico só, e é o pico que varia (moda 3 a 9, sd=2,12): o mecanismo por trás do `hor_m` disperso da nota 15, visto agora em ação — coevolução dos outros traços apaga a bimodalidade visível, não a existência estrutural do ponto. Agregados em `datasets/bimodalidade.csv` | 2 — bem posicional (fecha a sonda pós-nota-20) |
| [24](./24-o-detector-de-colapso.md) | **eixo microscópio, primeiro módulo: o detector de colapso não confabula, mas fica mudo diante do silêncio total**: um detector que só vê a própria história de população (janela recente × referência) teve **0/8 falsos positivos** em população saudável (o teste central do eixo — não confabula). No colapso real forte (imposto ligando no meio da corrida), detectou **6/8 seeds** em ~54 ticks de latência — mas as outras **2/8 sofreram extinção total em 11–14 ticks**, rápido demais para a janela de 250 ticks sequer refletir a queda: não é falso negativo, é o detector ficando mudo pela mesma razão estrutural do eremita mudo da nota 06 (não há biografia a narrar quando o sujeito desaparece antes da régua olhar). Das seeds que sobrevivem a um choque removido depois, **100% desligam** o sinal quando a população se recupera — o relato acompanha a transição, não fica preso numa narrativa. Agregados em `datasets/detector-colapso.csv` | eixo microscópio (1º módulo) |
| [25](./25-a-dose-resposta-do-detector.md) | **a dose-resposta do detector de colapso**: aprofunda o limite da nota 24. D1 — não é dose-resposta suave: 0/8 seeds se extinguem em todo custo ≤0,25 (com latência e queda_max escalando limpo e monotonicamente com a severidade), e 2/8 somem em `c=0,30` — uma **transição de fase estreita**, não uma cauda esperada. D2 — trocar a janela de 250 para 30 ticks pega **1 seed a mais** (dispara 3 ticks antes da extinção completa) **sem custar nenhum falso positivo adicional** (0/8 nos dois tamanhos), mas não resolve por completo: uma seed fica a `queda_max=0,1984` — a um triz do limiar de 0,20 — sem nunca disparar. Agregados em `datasets/dose-detector.csv` e `datasets/janela-detector.csv` | eixo microscópio (aprofunda o 1º módulo) |
| [26](./26-duas-linhagens-sem-h-conhecido.md) | **"há duas linhagens distintas?" sem conhecer `h*` de antemão**: generaliza a bimodalidade da nota 23 com um algoritmo que DESCOBRE o vale sozinho (compara cada bin com o máximo corrido de cada lado, não só o vizinho — corrige o defeito de ruído que a própria nota 23 descartou). Concorda com a nota 23 nos casos claros (δ≥0,90: 6-7/8), e a demonstração ao vivo (portada para dentro do `main.c`) acha o vale exatamente no `h*` teórico sem ser informado. Mas em δ=0,80 **discorda em massa** (0/8 contra 4/8 da nota 23) — o histograma é uma cauda enviesada rumo ao teto, não dois picos separados, o que **resolve a favor de "carga de mutação"** a ambiguidade que a nota 23 tinha deixado explicitamente em aberto. Reanalisa `datasets/bimodalidade.csv`, sem dataset novo | eixo microscópio (2º módulo) |

## Erratas

Notas são datadas, não sagradas. Quando um achado posterior derruba um número de
uma nota anterior, a nota antiga recebe um bloco **Errata** apontando para a nova —
o texto original **não é reescrito para parecer que sempre esteve certo**. A nota 02
corrigiu as comparações de população da nota 01; a nota 01 diz isso, em voz alta,
no lugar onde errou.
